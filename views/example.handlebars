<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">{{recipe.recipe_name}}</h5>
          <p>
            <strong>Ingredients</strong>: {{recipe.ingredients}}</p>
          <p>
          <p>
            <strong>Directions</strong>: {{recipe.directions}}</p>

            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
